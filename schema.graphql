type LiquidityMigration @entity {
  id: ID!
  staked: Int!
}

type AdapterV1 @entity {
  id: ID!
  name: String!
  staked: Int!
}

type StakedEvent @entity {
  id: ID!
  staker: String!
  adapter: String!
  strategy: String!
}

type StrategyToken @entity {
  id: ID!
  amount: BigDecimal!
}
